// This README contains instructions on how to set up a dev environment and how to deploy to the staging and production servers.

// After downloading the master branch from github...

// install dependancies
$ npm install

// create a .env file in the root of your repo
$ touch .env

// Contact the owner of the repo to get the necessary environment variables and their values

// Initialize git
$ git init
$ git add -A
$ git commit



// To start the dev server
$ concurrently --kill-others "ng serve" "nodemon server.js"
